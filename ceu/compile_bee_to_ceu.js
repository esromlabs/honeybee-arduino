// compile a BeeGraph to a ceu program.
// convert BeeGraph nodes to ceu blocks awakened by messages.
// convert BeeGraph edges to cue blocks that emit events.

module.b2c = function() {
  var compile = function (a) {
    var ceu_src = '';
    // convert BeeGraph to ceu
    
    return ceu_src;
  };
  return {"compile":compile};
};
